version: "3.8"

services:
  ai-server:
    build: .
    container_name: grpc_ai_server
    ports:
      - "50051:50051"
    networks:
      - my_network
    command: ["python", "grpc/predict_sign_server.py"] 

networks:
  my_network:
    driver: bridge
