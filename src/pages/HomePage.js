import CustomStyles from "@/styles/CustomStyles";

const HomePage = () => {
  return (
    <div>
      <div
        style={{ ...CustomStyles.fontHead28, color: CustomStyles.primaryBlue }}
      >
        주문하기
      </div>
    </div>
  );
};

export default HomePage;
